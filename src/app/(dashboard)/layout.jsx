import Saved from "@/components/sidebar";

export default async function RootLayout({children}){
    return (
        <div className="">
             
            {children}
            
        </div>
    )
}